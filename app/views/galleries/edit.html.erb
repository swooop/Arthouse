<h2>Edit a gallery</h2>
<%= form_for @gallery, url: gallery_path(@gallery), html: { multipart: true, method: 'put' } do |f| %>
  <%= render partial: 'fields', locals: { f: f } %>
  <%= f.submit "Submit", class: 'button radius' %>
<% end %>